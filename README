Steps
-----

1. Clone this Github Gist by:
       git clone https://gist.github.com/9e6cec4bb0f8ee826fcd.git

2. Launch the Simulated Annealer for comparing the success probabilities by using
   the following command:
       bash launch.sh -p
   or
       bash launch.sh --probability

   Launch the Simulated Annealer to note the running time by using the following command:
       bash launch.sh -t
   or
       bash launch.sh --time